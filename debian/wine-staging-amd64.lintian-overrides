# Wine preloader needs static linking to reserve memory and work properly, see
# the comments at loader/preloader.c for a fuller explanation
wine-staging-amd64 binary: statically-linked-binary opt/wine-staging/bin/wine64-preloader

# This is a bug in debhelper. It could be prevented by overriding the
# dh_makeshlibs target in debian/rules, but that will make the build fail.
wine-staging-amd64 binary: postinst-has-useless-call-to-ldconfig
wine-staging-amd64 binary: postrm-has-useless-call-to-ldconfig
